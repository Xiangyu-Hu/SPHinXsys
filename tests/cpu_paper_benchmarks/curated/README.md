# Curated benchmark results

Store only reviewed, compact paper inputs here, such as aggregate CSV files,
plot specifications, and provenance notes. Raw per-run output belongs under the
active build tree in `<build-dir>/benchmark_results/` and is intentionally
ignored by git.
